spring.application.name=Application
server.port=8080

# see application-mysql.properties, and application-h2.properties

# Switch between "dev" (H2) and "mysql" (Docker) here:
spring.profiles.active=h2
# spring.profiles.active=mysql